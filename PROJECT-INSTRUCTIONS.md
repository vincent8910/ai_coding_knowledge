# 專案指令檔教學：讓 AI 自動記住你的規矩

> **就像新人報到第一天，桌上放著一本「公司須知」——AI 打開專案，就自動知道該遵守什麼規則。**

---

## 大綱

- [專案指令檔是什麼？](#專案指令檔是什麼) — 新人入職手冊的概念
- [為什麼需要它？](#為什麼需要它) — 解決「每次都要重複交代」的問題
- [各工具的指令檔對照](#各工具的指令檔對照) — CLAUDE.md vs GEMINI.md vs .cursorrules
- [動手做：建立你的第一個指令檔](#動手做建立你的第一個指令檔) — 5 分鐘搞定
- [實戰範例：10 個指令檔模板](#實戰範例10-個指令檔模板) — 直接複製貼上
- [進階技巧](#進階技巧) — 團隊共用、多層繼承
- [下一步](#下一步)

---

## 專案指令檔是什麼？

### 先想像一個場景

> 你公司新來了一個實習生。每次交辦工作，你都要先說：
>
> 「我們的文件要用繁體中文、語氣要專業但親切、標題用 ## 開頭、每份文件結尾要加版權聲明、程式碼風格用這個⋯⋯」
>
> **每次。都要。重講。**

專案指令檔就是解決這個問題的。

**你只要在專案資料夾裡放一個特定的檔案，AI 每次打開這個專案時，就會自動讀取裡面的規則。**

就像你在新人的桌上放了一本「公司須知」——他不用你交代，自己就知道規矩。

### 沒有指令檔 vs 有指令檔

| 情境 | 沒有指令檔 😩 | 有指令檔 😎 |
|------|-------------|------------|
| 每次開專案 | 「記得用繁體中文⋯⋯」 | AI 自動知道 |
| 新同事加入 | 要把所有規矩口頭傳授 | 他打開專案就自動套用 |
| 風格不一致 | A 寫的跟 B 寫的長不一樣 | 全團隊同一套規則 |
| 換了 AI 工具 | 要重新教 | 指令檔格式跨工具通用 |

---

## 為什麼需要它？

1. **不用重複交代**：AI 每次啟動都會自動讀取規則
2. **團隊一致性**：所有人用同一份指令檔，產出風格統一
3. **可以 Git 管理**：指令檔可以跟專案一起版本控制、code review
4. **跨工具通用**：基本概念相同，只是檔名不同

---

## 各工具的指令檔對照

| 工具 | 檔名 | 放在哪裡 |
|------|------|---------|
| **Claude Code** | `CLAUDE.md` | 專案根目錄 |
| **Claude Code**（個人全域） | `CLAUDE.md` | `~/.claude/CLAUDE.md` |
| **[Antigravity](terms/antigravity.md)** | `GEMINI.md` 或 `AGENTS.md` | 專案根目錄 |
| **Cursor** | `.cursorrules` | 專案根目錄 |
| **[Codex](terms/codex.md)** | `AGENTS.md` | 專案根目錄 |

> 💡 **內容格式都是 Markdown**，寫法幾乎一樣。只是不同工具認不同的檔名。

---

## 動手做：建立你的第一個指令檔

> 我們以 Claude Code 為例，建一個 `CLAUDE.md`。如果你用 Antigravity，改成 `GEMINI.md` 就好。

### Step 1：在專案根目錄建檔

在你的專案資料夾（最外層）新建一個檔案，取名叫 `CLAUDE.md`。

### Step 2：寫入基本規則

用記事本打開，貼上以下內容：

```markdown
# CLAUDE.md

## 基本規則

- 所有回覆和文件使用**繁體中文（台灣用語）**
- 語氣專業但親切，避免過度生硬
- 不確定的事情要先問，不要自己猜

## 文件格式

- 標題使用 ## 開頭（不用 #，# 留給檔案標題）
- 段落之間空一行
- 程式碼用 ``` 包起來，標明語言

## 不要做的事

- 不要自動 push 到 GitHub
- 不要修改 .env 檔案
- 不要刪除任何檔案，除非我明確要求
```

### Step 3：測試效果

開啟 Claude Code（或你的 AI 工具），進入這個專案資料夾。

試著輸入：

```
幫我寫一份會議紀錄的模板
```

看看 AI 是不是自動用繁體中文、用你指定的格式回覆了？

**恭喜！你的 AI 現在「認識」這個專案了！** 🎉

---

## 實戰範例：10 個指令檔模板

> 以下模板可以直接複製貼上到你的 `CLAUDE.md`（或 `GEMINI.md`）裡。

### 1. 行銷團隊專案

```markdown
# CLAUDE.md

## 團隊資訊
- 我們是行銷部門，主要負責品牌推廣和內容行銷
- 目標受眾：25-40 歲的台灣上班族

## 寫作風格
- 語言：繁體中文，台灣用語（「軟體」非「軟件」）
- 語氣：輕鬆活潑但不失專業
- 避免使用行銷術語（如 "synergy"、"leverage"），用白話說

## 內容規範
- 社群貼文長度：Facebook 200 字以內、IG 100 字以內
- 標題要有吸引力，可以用問句或數字開頭
- 每篇文章都要有明確的 CTA（呼籲行動）
```

### 2. PM 專案管理

```markdown
# CLAUDE.md

## 專案資訊
- 專案名稱：[你的專案名]
- 目前階段：開發中 / 測試中 / 上線中
- 團隊規模：X 人

## 文件規範
- 所有規格文件使用 Markdown 格式
- 日期格式：YYYY-MM-DD
- 任務狀態標記：⬜ 待做 / 🔄 進行中 / ✅ 完成 / ❌ 取消

## 溝通規則
- 回報格式：先結論、再細節
- 風險要主動標記，不要藏著
- 每份文件標明負責人和最後更新日期
```

### 3. 資料分析團隊

```markdown
# CLAUDE.md

## 資料規範
- 數字格式：千分位用逗號（1,234,567）
- 金額單位：預設台幣（NTD），外幣要標明
- 百分比：保留小數一位（12.3%）
- 日期格式：YYYY-MM-DD

## 分析規則
- 所有數據分析必須附上資料來源
- 比較時要標明基準期間（vs 去年同期 / vs 上月）
- 異常值要用 ⚠️ 標記並說明原因

## 輸出格式
- 先給 3 行以內的重點摘要
- 再給詳細分析
- 最後給可行建議（至少 2 個）
```

### 4. 客服 / 行政團隊

```markdown
# CLAUDE.md

## 語氣規範
- 對客戶：尊稱「您」，語氣溫和有禮
- 對內部：可以用「你」，簡潔明瞭
- 避免使用過多驚嘆號

## Email 規範
- 結構：問候 → 主旨 → 說明 → 行動呼籲 → 結尾
- 簽名格式：姓名 / 職稱 / 分機
- 回覆模板分兩版：正式版（客戶）和輕鬆版（同事）

## 文件整理
- 檔名格式：YYYYMMDD_文件名稱_版本號
- 重要文件要標注機密等級
```

### 5. 軟體開發團隊

```markdown
# CLAUDE.md

## 程式碼風格
- 語言：TypeScript 為主
- 縮排：2 spaces（不用 tab）
- 命名：變數用 camelCase，常數用 UPPER_SNAKE_CASE
- 函數要寫簡短的 JSDoc 註解

## Git 規則
- Commit message 用中文，格式：「動詞 + 做了什麼」
- 不要自動 push，讓我確認後再推
- Branch 命名：feature/功能名 或 fix/問題名

## 安全規則
- 絕對不要把 API Key 寫在程式碼裡
- 不要修改 .env 和 .env.local
- 資料庫操作前先確認是不是 production
```

### 6. 教育 / 培訓團隊

```markdown
# CLAUDE.md

## 教學風格
- 對象：完全零基礎的學員
- 每個技術名詞第一次出現時要用白話解釋
- 大量使用生活比喻
- 步驟要非常詳細，不跳步

## 格式規範
- 每個段落不超過 3 行
- 重點用粗體標記
- 操作步驟用編號列表
- 每個章節結尾加一個小測驗或練習
```

### 7. 法務 / 合規團隊

```markdown
# CLAUDE.md

## 文件規範
- 語言精確，避免模糊用語
- 法律條文引用要標明法規名稱、條次
- 日期和金額不可省略
- 重要條款用粗體標注

## 安全規則
- 不處理真實的客戶個資（用假名替代）
- 合約內容不外傳
- 分析結果加上免責聲明
```

### 8. 設計團隊

```markdown
# CLAUDE.md

## 設計規範
- 品牌色：主色 #FF6B00、輔色 #2C3E50
- 字體：標題用 Noto Sans TC Bold，內文用 Regular
- 間距：元素間距最少 16px
- 圖片比例：社群用 1:1，Banner 用 16:9

## 文案風格
- 簡短有力，每個標題不超過 10 個字
- 多用動詞開頭
```

### 9. 業務 / 銷售團隊

```markdown
# CLAUDE.md

## 報價規範
- 金額用台幣，含稅標明「含稅」
- 報價單有效期限：30 天
- 折扣不超過 15%，超過需主管核准

## 客戶溝通
- 回信速度：24 小時內
- 語氣：積極正面、展現專業
- 競品比較時客觀呈現，不貶低對手
```

### 10. 多語系專案

```markdown
# CLAUDE.md

## 語言規範
- 預設語言：繁體中文（台灣）
- 支援語言：英文、日文、簡體中文
- 翻譯時保持品牌名稱不翻（例如「Claude Code」不翻成「克勞德代碼」）
- 技術名詞附上英文原文（例如：應用程式介面（API））

## 格式
- 中英文之間加半形空格（例如「使用 AI 工具」）
- 數字用半形（123 不是１２３）
```

---

## 進階技巧

### 多層指令檔

Claude Code 支援多層指令檔，就像公司有集團守則、部門守則、專案守則：

```
~/.claude/CLAUDE.md          ← 你的個人全域規則（所有專案都套用）
專案根目錄/CLAUDE.md         ← 專案規則（只有這個專案套用）
專案/子資料夾/CLAUDE.md      ← 子目錄規則（更細的範圍）
```

**優先級**：子目錄 > 專案 > 全域（越近的規則越優先）

### 團隊共用

把 `CLAUDE.md` 加入 Git 版控，所有團隊成員 pull 下來就自動套用：

```bash
git add CLAUDE.md
git commit -m "新增 AI 指令檔，統一團隊工作規範"
git push
```

這樣每個人的 AI 都遵守同一套規則，產出風格自然統一。

### 搭配 Skill 使用

指令檔定義「通用規則」，[Skill](terms/skill.md) 定義「特定任務的做法」——兩者搭配是最強組合：

| | 專案指令檔 | Skill |
|---|---|---|
| **定義什麼** | 通用規則（語言、格式、禁止事項） | 特定任務的做法（怎麼分析報表） |
| **何時生效** | AI 打開專案就自動載入 | 你呼叫 `/skill名稱` 時才啟動 |
| **比喻** | 公司規章 | 工作 SOP |

---

## 下一步

建好指令檔後，搭配 [Skill](SKILLS.md) 使用效果加倍！

---

**[← 回到首頁](README.md)** · **[什麼是專案指令檔？](terms/project-instructions.md)** · **[Skill 教學 →](SKILLS.md)**

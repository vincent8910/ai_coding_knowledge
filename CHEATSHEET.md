# 指令速查卡：Claude Code 小抄

> **印出來貼在螢幕旁邊，或存在手機裡隨時查。**

---

## 基礎操作：日常三招

| 你想做的事 | 指令 | 說明 |
|-----------|------|------|
| 啟動 Claude Code | `claude` | 在 Terminal 輸入，開始對話 |
| 在特定資料夾啟動 | `claude` （先 `cd` 到目標資料夾） | 讓 AI 能看到那個資料夾的檔案 |
| 結束對話 | `/exit` 或 `Ctrl + C` | 下班收工 |
| 清除對話記錄 | `/clear` | 從頭開始新對話 |
| 查看幫助 | `/help` | 看所有可用指令 |

---

## 怎麼跟 AI 說話最有效？

Claude Code 聽得懂中文，但**你說得越清楚，它做得越好**。就像交辦工作給同事——越具體越好。

### ❌ 模糊的說法

```
幫我處理那個檔案
```

### ✅ 清楚的說法

```
幫我讀取 report.csv，把裡面的英文欄位翻譯成繁體中文，然後存成 report_zh.csv
```

### 黃金公式：任務 + 對象 + 條件 + 輸出

| 元素 | 說明 | 範例 |
|------|------|------|
| **任務** | 你要它做什麼 | 「翻譯」、「整理」、「分析」 |
| **對象** | 對什麼東西動手 | 「report.csv」、「這個資料夾的所有 .txt」 |
| **條件** | 有什麼特殊要求 | 「用繁體中文」、「只保留前 10 筆」 |
| **輸出** | 結果要長什麼樣 | 「存成新檔案」、「用表格顯示」 |

### 實用對話範例

```
📌 翻譯任務
「把 notes.md 從英文翻譯成繁體中文，保持 Markdown 格式不變」

📌 摘要任務
「讀取 meeting_notes/ 資料夾裡所有的 .md 檔案，幫我產出一份重點摘要」

📌 分析任務
「分析 sales_data.csv，告訴我每個月的銷售趨勢，用表格呈現」

📌 整理任務
「把 inbox/ 資料夾裡的 50 個檔案，按照日期重新命名成 YYYY-MM-DD 格式」

📌 撰寫任務
「根據 project_spec.md 的內容，幫我寫一份給客戶的專案進度報告」
```

---

## SuperClaude 指令速查表

> 💡 以下指令需要先安裝 [SuperClaude](https://github.com/NomenAK/SuperClaude) 才能使用。
> 沒裝也沒關係，Claude Code 本身就很強了，SuperClaude 是「加裝渦輪增壓」。

SuperClaude 的指令都以 `/sc:` 開頭，就像點餐時的「套餐代號」：

### 常用指令 TOP 10

| 指令 | 做什麼 | 比喻 |
|------|--------|------|
| `/sc:init` | 初始化專案，建立 CLAUDE.md | 新員工報到，認識辦公環境 |
| `/sc:load` | 載入專案上下文和記憶 | 員工翻開上次的工作筆記 |
| `/sc:save` | 儲存當前工作狀態 | 員工把今天的進度存檔 |
| `/sc:analyze` | 深度分析程式碼或文件 | 請顧問做完整健檢報告 |
| `/sc:improve` | 改善現有程式碼或文件 | 請編輯幫你潤稿 |
| `/sc:explain` | 用白話解釋複雜的東西 | 請老師用小學生聽得懂的方式說 |
| `/sc:plan` | 制定執行計畫 | 專案經理排工作時程 |
| `/sc:review` | 審查程式碼或文件品質 | 品管部門做最終檢查 |
| `/sc:build` | 建立新功能或元件 | 工程師動手蓋東西 |
| `/sc:research` | 深度研究特定主題 | 研究員去圖書館挖資料 |

### 其他實用指令

| 指令 | 做什麼 |
|------|--------|
| `/sc:design` | 設計系統架構或介面 |
| `/sc:codegen` | 產生程式碼骨架 |
| `/sc:test` | 撰寫或執行測試 |
| `/sc:doc` | 產生技術文件 |
| `/sc:migrate` | 協助程式碼遷移 |
| `/sc:scan` | 掃描安全性問題 |
| `/sc:estimate` | 估算工作量和複雜度 |
| `/sc:commit` | 產生有意義的 Git commit 訊息 |
| `/sc:deploy` | 協助部署流程 |
| `/sc:troubleshoot` | 系統性排查問題 |
| `/sc:configure` | 設定專案環境 |
| `/sc:monitor` | 監控系統狀態 |
| `/sc:cleanup` | 清理程式碼或專案結構 |
| `/sc:business-panel` | 啟動多專家商業分析 |
| `/sc:create-agent` | 建立自訂 AI 代理人 |
| `/sc:spawn-agent` | 啟動 AI 代理人執行任務 |

---

## MCP 伺服器速查：AI 員工的感官器官

MCP（Model Context Protocol）就是 AI 的**感官器官**——讓它不只會想，還能看、聽、摸。

| MCP 伺服器 | 它是什麼感官 | 白話說 |
|-----------|------------|--------|
| **Context7** | 📚 專業圖書館員 | 查官方文件和技術手冊，確保用法正確 |
| **Sequential** | 🧠 深度思考模式 | 複雜問題拆解成小步驟，一步步推理 |
| **Serena** | 🗂️ 長期記憶 + 程式碼導航 | 記住專案結構、跨對話保留記憶 |
| **Magic** | 🎨 UI 設計師 | 快速生成漂亮的前端介面元件 |
| **Morphllm** | ✂️ 批次編輯器 | 一次改大量檔案裡的相同模式 |
| **Playwright** | 👁️ 瀏覽器操控 | 自動化操作網頁、截圖、測試 |
| **Tavily** | 🔍 即時搜尋引擎 | 搜尋最新的網路資訊和新聞 |
| **Chrome DevTools** | 🔬 瀏覽器顯微鏡 | 檢查網頁效能、除錯、分析 |

> 💡 **你不需要記住這些！** MCP 在背後自動運作。
> 就像你不需要知道胃怎麼消化食物，你只要吃就好。

---

## 旗標速查：給 AI 的特殊指示

旗標（Flag）就像跟服務生說「少冰」「去冰」——在指令後面加上旗標，告訴 AI 要用什麼方式工作。

### 思考深度旗標

| 旗標 | 效果 | 什麼時候用 |
|------|------|-----------|
| `--think` | 標準深度分析 | 一般的分析任務 |
| `--think-hard` | 深入分析 | 複雜的架構問題 |
| `--ultrathink` | 最深度分析 | 超級複雜的系統設計 |

**用法範例：**
```
分析這個專案的結構 --think
```

### 工作模式旗標

| 旗標 | 效果 | 什麼時候用 |
|------|------|-----------|
| `--brainstorm` | 腦力激盪模式 | 還沒想清楚要做什麼時 |
| `--introspect` | 自我反思模式 | 要 AI 解釋它的思考過程 |
| `--loop` | 反覆改善模式 | 要 AI 把東西越改越好 |
| `--validate` | 驗證模式 | 要 AI 在執行前先檢查風險 |

### MCP 啟用旗標

| 旗標 | 啟用什麼 |
|------|---------|
| `--c7` | Context7（查文件） |
| `--seq` | Sequential（深度推理） |
| `--serena` | Serena（專案記憶） |
| `--tavily` | Tavily（網路搜尋） |
| `--play` | Playwright（瀏覽器操控） |

---

## 危險操作警示區 🚨

Claude Code 很強大，但能力越大，責任越大。以下操作請**特別小心**：

### 🔴 高風險（三思而後行）

| 操作 | 為什麼危險 | 安全做法 |
|------|-----------|---------|
| 刪除檔案 | 刪了就沒了 | 先確認檔案名稱，有 Git 就先 commit |
| 修改設定檔 | 可能讓系統出問題 | 先備份原始檔案 |
| 批次重新命名 | 改錯就很難復原 | 先在小範圍測試 |
| 執行系統指令 | 可能影響電腦運作 | 看不懂的指令就按 n（拒絕） |

### 🟡 中風險（留意一下）

| 操作 | 注意事項 |
|------|---------|
| 大量檔案編輯 | 先用 Git 建立還原點 |
| 安裝套件 | 確認來源可信 |
| 網路操作 | 注意不要外洩機密資訊 |

### 保命心法

1. **看不懂就說 No**——Claude Code 問你 `Allow? (y/n)` 時，不確定就按 `n`
2. **重要檔案先備份**——要修改重要東西前，先手動複製一份
3. **用 Git 當後悔藥**——養成 `git commit` 的習慣，隨時可以回到過去
4. **小範圍先試**——批次操作前，先用一兩個檔案測試效果

---

## 快速參考卡（可以截圖存手機）

```
┌─────────────────────────────────────────────┐
│         Claude Code 每日必備指令              │
├─────────────────────────────────────────────┤
│  啟動      │  claude                         │
│  離開      │  /exit 或 Ctrl+C                │
│  清除對話  │  /clear                         │
│  查幫助    │  /help                          │
├─────────────────────────────────────────────┤
│         跟 AI 說話的公式                      │
│  任務 + 對象 + 條件 + 輸出                    │
│  例：翻譯 report.md 成繁中 存成 report_zh.md  │
├─────────────────────────────────────────────┤
│         遇到問題時                            │
│  看不懂就按 n ─ 不確定就問 ─ 重要的先備份     │
└─────────────────────────────────────────────┘
```

---

## 下一步

小抄在手，信心滿滿！去看看實際能做什麼 👉 **[使用場景 →](USE_CASES.md)**

想複習安裝步驟？👉 **[安裝指南 →](INSTALL.md)**
